const { Router } = require('express');
const { Cookies } = require('../models/Cookies');

module.exports = Router()
  .get('/:id', async (req, res, next) => {
    try {
      const data = await Cookies.getById(req.params.id);
      if (!data) {
        next();
      }
      res.json(data);
    } catch (e) {
      next(e);
    }
  })
  .get('/', async (req, res, next) => {
    try {
      const data = await Cookies.getAll();
      res.json(data);
    } catch (e) {
      next(e);
    }
  })
  .post('/', async (req, res, next) => {
    try {
      const data = await Cookies.insert(req.body);
      res.json(data);
    } catch (e) {
      next(e);
    }
  })
  .put('/:id', async (req, res, next) => {
    try {
      const data = await Cookies.updateById(req.params.id, req.body);
      res.json(data);
    } catch (e) {
      next(e);
    }
  })
  .delete('/:id', async (req, res, next) => {
    try {
      const data = await Cookies.delete(req.params.id);
      res.json(data);
    } catch (e) {
      next(e);
    }
  });
